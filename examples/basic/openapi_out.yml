# Specification generated by echOpen

openapi: 3.2.0
jsonSchemaDialect: https://spec.openapis.org/oas/3.2/dialect/2025-09-17
info:
    title: Basic Example
    version: 1.0.0
    description: Test API for basic usage of echOpen.
    license:
        name: MIT
        url: https://example.com/license
tags:
    - name: hello_world
      description: Hello World API Routes
    - name: param
      description: Routes with params
paths:
    /hello:
        get:
            operationId: getHello
            tags:
                - hello_world
            responses:
                "200":
                    description: Successful response
    /hello/{id}:
        get:
            operationId: getHelloById
            tags:
                - hello_world
                - param
            parameters:
                - name: id
                  in: path
                  description: ID Parameter
                  required: true
                  schema:
                    type: integer
                  examples:
                    - value: 1234
            responses:
                "200":
                    description: Successful response
    /hello/body:
        patch:
            operationId: patchHelloBody
            tags:
                - hello_world
            requestBody:
                description: Body params
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RequestBody'
            responses:
                "200":
                    description: Successful response
    /hello/query:
        get:
            operationId: getHelloQuery
            tags:
                - hello_world
            parameters:
                - name: offset
                  in: query
                  description: Offset into results
                  style: form
                  schema:
                    type: integer
                - name: limit
                  in: query
                  style: form
                  schema:
                    type: integer
            responses:
                "200":
                    description: Successful response
components:
    schemas:
        RequestBody:
            type: object
            properties:
                email:
                    description: Optional email address
                    examples:
                        - joe_bloggs@example.com
                    type: string
                first_name:
                    description: User first name
                    examples:
                        - Joe
                    type: string
                last_name:
                    description: User last name
                    examples:
                        - Bloggs
                    type: string
                meta:
                    type: object
                    properties:
                        terms_and_conditions:
                            description: Date of T&Cs acceptance
                            type: integer
                settings:
                    $ref: '#/components/schemas/RequestBodySettings'
        RequestBodySettings:
            type: object
            required:
                - enabled
                - other
            properties:
                enabled:
                    description: Enabled flag
                    type: boolean
                other:
                    type: object
