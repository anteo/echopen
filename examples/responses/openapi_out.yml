# Specification generated by echOpen

openapi: 3.1.0
jsonSchemaDialect: https://spec.openapis.org/oas/3.1/dialect/base
info:
    title: Responses
    version: 1.0.0
    description: Example to show reused responses, loosely based on the Petstore examples.
tags:
    - name: pets
      description: Pets endpoints
paths:
    /pets:
        get:
            operationId: findPets
            description: Finds all pets the user has access to
            tags:
                - pets
            parameters:
                - name: tags
                  in: query
                  description: tags to filter by
                  style: form
                  schema:
                    type: array
                    items:
                        type: string
                - name: limit
                  in: query
                  description: maximum number of results to return
                  style: form
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: pet response
                    content:
                        application/json:
                            schema:
                                type: array
                                items:
                                    $ref: '#/components/schemas/Pet'
                default:
                    $ref: '#/components/responses/UnexpectedErrorResponse'
        post:
            operationId: addPet
            description: Creates a new pet in the store. Duplicates are allowed
            tags:
                - pets
            requestBody:
                description: Pet to add to the store
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/NewPet'
            responses:
                "200":
                    $ref: '#/components/responses/PetResponse'
                default:
                    $ref: '#/components/responses/UnexpectedErrorResponse'
    /pets/{id}:
        get:
            operationId: findPetByID
            description: Returns a user based on a single ID, if the user does not have access to the pet
            tags:
                - pets
            parameters:
                - name: id
                  in: path
                  description: ID of pet to fetch
                  required: true
                  schema:
                    type: integer
                    format: int64
            responses:
                "200":
                    $ref: '#/components/responses/PetResponse'
                "404":
                    $ref: '#/components/responses/NotFoundResponse'
                default:
                    $ref: '#/components/responses/UnexpectedErrorResponse'
        delete:
            operationId: deletePet
            description: deletes a single pet based on the ID supplied
            tags:
                - pets
            parameters:
                - name: id
                  in: path
                  description: ID of pet to delete
                  required: true
                  schema:
                    type: integer
                    format: int64
            responses:
                "204":
                    description: pet deleted
                "404":
                    $ref: '#/components/responses/NotFoundResponse'
                default:
                    $ref: '#/components/responses/UnexpectedErrorResponse'
components:
    schemas:
        Error:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
        NewPet:
            type: object
            properties:
                name:
                    type: string
                tag:
                    type: string
        Pet:
            allOf:
                - $ref: '#/components/schemas/NewPet'
                - type: object
                  properties:
                    id:
                        type: integer
                        format: int64
    responses:
        NotFoundResponse:
            description: not found
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
        PetResponse:
            description: pet response
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Pet'
        UnexpectedErrorResponse:
            description: unexpected error
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
