# Specification generated by echOpen

openapi: 3.1.0
jsonSchemaDialect: https://spec.openapis.org/oas/3.1/dialect/base
info:
    title: Parameters
    version: 1.0.0
    description: |-
        Parameters
        ==========

        Example including path and query parameters.
    license:
        name: MIT
        url: https://example.com/license
tags:
    - name: params
      description: Params API Routes
paths:
    /params/{id}:
        get:
            operationId: getParamsById
            tags:
                - params
            parameters:
                - name: id
                  in: path
                  description: ID Parameter
                  required: true
                  schema:
                    type: integer
                - name: notes
                  in: query
                  description: Optional notes to include in response
                  style: form
                  schema:
                    type: string
            responses:
                "200":
                    description: Default response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ValidResponseBody'
                "400":
                    description: Bad request
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorResponseBody'
                "404":
                    description: Not found
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorResponseBody'
components:
    schemas:
        ErrorResponseBody:
            type: object
            required:
                - code
            properties:
                code:
                    description: Error code
                    type: string
        ValidResponseBody:
            type: object
            properties:
                id:
                    type: integer
                notes:
                    type: string
